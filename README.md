## Romantic Valentine’s Day single-page website (7 sections, pastel aesthetic, animations, gallery, surprise + optional music)

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
